import "./style.css";

export const metadata = {
 title: "About | Exchange Platform",
};

export default function AboutLayout({
 children,
}: {
 children: React.ReactNode;
}) {
 return <>{children}</>;
}
